using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Chapter5Manager : MonoBehaviour
{
    /* 条件
     * 1. 二次元配列を用いた状態遷移表を使用すること
     * 2. ボタンイベントに関して、MVPパターンを使用すること
     * 　 なお、Button.OnClickは.AddListenerでコールバックを登録することができる
     * 3. ボタンはA, B, C, D, Eの5つである
     * 4. Statusは、デフォルトでは何も表示しない
    */
    /* マネージャクラスの動作
     * 実装
     * 1. 任意の特定の順番でA~Eのボタンが押された場合Statusに"解錠"と出力し、間違った場合は"拒否"と出力する
     */
}
